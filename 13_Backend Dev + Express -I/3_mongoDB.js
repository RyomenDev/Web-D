
MONGO DB:
    no-SQL database
    store data in JSON, BSON format
    store data in collections, key-value pairs , documents, graph

    MongoDB is a document-oriented database program.
    MongoDB uses JSON-like documents with optional schemas.
    MongoDB is a cross-platform document-oriented database program.
    
    MongoDB is a source-available cross-platform document-oriented database program.
    Classified as a NoSQL database program, MongoDB uses JSON-like documents with optional schemas.
    
-------------------------------------------------

SQL: table
no-SQL: no table

-------------------------------------------------

CRUD:{create , read , update , delete}
set-up 
    1. install mongoDB
    2. open cmd
    3. type "mongod"
    4. open another cmd
    5. type "mongo"
    6. type "show dbs"
    7. type "use <database name>"
    8. type "db"
    9. type "show collections"
    10. type "db.<collection name>.insertOne({<data>})"
    11. type "db.<collection name>.find()"
    12. type "db.<collection name>.updateOne({<condition>},{$set:{<data>}})"
    13. type "db.<collection name>.deleteOne({<condition>})"
    14. type "db.<collection name>.drop()"
    15. type "exit"
    16. type "quit"
    
-------------------------------------------------

mongoDB compass : graphical user interface (GUI) for mongoDB (for CRUD)

-------------------------------------------------

