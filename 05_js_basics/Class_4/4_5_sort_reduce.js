// sort an array of objects

// for-of loop on iterables like array

let arr1 = [
  { id: 3, name: "pqr" },
  { id: 1, name: "abc" },
  { id: 2, name: "xyz" },
];

// console.log(arr1.sort((a, b) => a.id - b.id));

//

//

// reduce in an array
let arr2 = [1, 2, 3, 4, 5, -1];

console.log(arr2.reduce((acc, curr) => acc + curr, 0));
// acc=accumulator ; curr=current value

